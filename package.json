{
  "private": true,
  "name": "tsdk-monorepo",
  "packageManager": "pnpm@7.27.1",
  "scripts": {
    "lint": "turbo lint",
    "test": "turbo test",
    "build": "turbo build",
    "dev": "turbo dev",
    "start-prepare": "chmod +x ./node_modules/.bin/husky && husky install && chmod +x ./node_modules/.bin/cross-env && chmod +x ./node_modules/.bin/nest"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.1.2",
    "turbo": "^1.8.3"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@babel/*",
        "expo-modules-*",
        "typescript"
      ]
    },
    "packageExtensions": {},
    "overrides": {
      "uuid": "^3.4.0"
    }
  },
  "prettier": {
    "printWidth": 100,
    "tabWidth": 2,
    "singleQuote": true,
    "trailingComma": "es5",
    "bracketSameLine": true
  },
  "lint-staged": {
    "*.{ts,tsx,mjs,js,mts}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{md,css}": [
      "prettier --write"
    ]
  }
}
